# Step 2: Chat Formatting & Loss Masking
# ติดตั้ง: pip install -r requirements.txt
#
# หมายเหตุ: unsloth ต้องติดตั้งแยกก่อนเสมอ เพราะมี dependency พิเศษ
# สำหรับ Google Colab:
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
#   pip install -r requirements.txt

# Core
torch>=2.1.0
transformers>=4.40.0

# Unsloth (ติดตั้งแยกตาม platform ด้านบน)
# unsloth

# Fine-tuning (เพิ่ม DataCollatorForCompletionOnlyLM ใน trl)
trl>=0.8.6
peft>=0.10.0
accelerate>=0.28.0
bitsandbytes>=0.43.0

# Data
datasets>=2.18.0
